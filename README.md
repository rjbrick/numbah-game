# In the folder where your files are
git init
git add index.html README.md
git commit -m "Initial commit — Numbah v1"
git branch -M main
git remote add origin https://github.com/rjbrick/numbah.git
git push -u origin main
